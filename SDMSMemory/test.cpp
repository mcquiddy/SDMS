#include "d_pointer_size_type.h"
#include "linked_list.h"

int main(){
    lista<d_pointer_size_type> pPointers;
    int i=0;
    while(i=10){
        d_pointer_size_type temp;
        temp.setReferences(i);
        pPointers.insert_head(d_pointer_size_type);
        i++;

}


